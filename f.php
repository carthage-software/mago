<?php

function f(int $a, int: int {
    $hello = 'Hello, World!'

    echo [$hello]

    foreach (range(0, 10) as $number) {
        echo foo($number . "\n");
    }

    unset($f
    foreach ([1 as $number) {
        display($number;
    }

    echo "Done!\n";
    try {}
}
